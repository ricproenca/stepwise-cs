# 11.2 Constructs

**Course:** Cambridge International AS & A Level Computer Science (9618)
**Source:** 11.2 Constructs.pdf

---

Cambridge (CIE) AS                                                         Your notes
Computer Science
Constructs
Contents
  Selection
  Iteration

              © 2026 Save My Exams, Ltd.   Get more and ace your exams at savemyexams.com   1

  Selection
                                                                                                     Your notes

Selection
What is selection?
       Selection is when the flow of a program is changed, depending on a set of conditions
       The outcome of this condition will then determine which lines or block of code is run
       next
       Selection is used for validation, calculation and making sense of a user's choices
       There are two ways to write selection statements:
           if... then... else...
           case...

If statements
What is an if statement?
       An If statements allow you to execute a set of instructions if a condition is true
       They have the following syntax:

                                             Pseudocode

 IF <condition> THEN                                    IF <condition> THEN
  <statement(s)>                                         <statement(s)>
 ENDIF                                                  ELSE
                                                         <statement(s)>
                                                        ENDIF

                Without an ELSE clause                             With an ELSE clause

Nested if statements
       Nested if statements are an if statement within an if statement
       Nested means to be 'stored inside the other'

Example code
IF Player2Score > Player1Score THEN
  IF Player2Score > HighScore THEN
    OUTPUT Player2, " is champion and highest scorer"
  ELSE
    OUTPUT Player2, " is the new champion"
  ENDIF
ELSE

                                   © 2026 Save My Exams, Ltd.      Get more and ace your exams at savemyexams.com   2

 OUTPUT Player1, " is still the champion"
 IF Player1Score > HighScore THEN
   OUTPUT Player1, " is also the highest scorer"                                                               Your notes
 ENDIF
ENDIF

If statements in different languages
 Python                                                                       VB.net

 weather = input("What is the weather like today? (sunny, rainy, snowy): ")   Module WeatherCheck
                                                                               Sub Main()
 if weather == "sunny":                                                          Dim weather As String
   print("Don't forget your sunglasses!")
 elif weather == "rainy":                                                        Console.Write("What is th
    print("Take an umbrella with you.")                                          weather = Console.ReadL
 elif weather == "snowy":
   print("Wear warm clothes!")                                                   If weather = "sunny" Then
 else:                                                                             Console.WriteLine("Don
   print("Not sure what to suggest for that kind of weather.")                   ElseIf weather = "rainy" Th
                                                                                   Console.WriteLine("Tak
                                                                                 ElseIf weather = "snowy" T
                                                                                   Console.WriteLine("Wea
                                                                                 Else
                                                                                   Console.WriteLine("Not
                                                                                 End If

                                                                                 Console.ReadLine() ' Keep
                                                                               End Sub
                                                                              End Module

Case statements
What is a case statement?
    A case statement can mean less code but it only useful when comparing multiple values
    of the same variable
    If statements are more flexible and are generally used more in languages such as Python
    The format of a CASE statement is:

                                                Pseudocode

 CASE of <identifier>                     CASE of <identifier>
  <value 1> : <statement1>                  <value 1> : <statement1>
         <statement2>                             <statement2>
         ...                                      ...
  <value 2> : <statement1>                  <value 2> : <statement1>
         <statement2>                             <statement2>
         ...                                      ...

                                  © 2026 Save My Exams, Ltd.           Get more and ace your exams at savemyexams.com       3

  ...                                       OTHERWISE : <statement1>
 ENDCASE                                          <statement2>
                                                 ...                                                          Your notes
                                           ENDCASE

                                           An OTHERWISE clause can be the last clause

Example code
DECLARE Direction : STRING

OUTPUT "Enter a direction (N, S, E, W):"
INPUT Direction

CASE OF Direction
  "N" : OUTPUT "You are heading North"
  "S" : OUTPUT "You are heading South"
  "E" : OUTPUT "You are heading East"
  "W" : OUTPUT "You are heading West"
  OTHERWISE : OUTPUT "Invalid direction entered"
ENDCASE

Case statements in different languages
 Python                                                                       VB.net

 weather = input("What is the weather like today? (sunny, rainy, snowy): ")   Module WeatherCheck
                                                                               Sub Main()
 match weather:                                                                  Dim weather As String
   case "sunny":
        print("Don't forget your sunglasses!")                                   Console.Write("What is th
   case "rainy":                                                                 weather = Console.ReadL
     print("Take an umbrella with you.")
   case "snowy":                                                                 Select Case weather
        print("Wear warm clothes!")                                                Case "sunny"
   case _:                                                                           Console.WriteLine("D
     print("Not sure what to suggest for that kind of weather.")                   Case "rainy"
                                                                                       Console.WriteLine("T
                                                                                   Case "snowy"
                                                                                     Console.WriteLine("W
                                                                                   Case Else
                                                                                       Console.WriteLine("N
                                                                                 End Select

                                                                                 Console.ReadLine() ' Keep
                                                                               End Sub
                                                                              End Module

                                      © 2026 Save My Exams, Ltd.       Get more and ace your exams at savemyexams.com      4

  Iteration
                                                                                                           Your notes

What is iteration?
    Iteration is repeating a line or a block of code using a loop
    Iteration can be:
           Count-controlled
           Condition-controlled

Count-controlled loops
What is a count-controlled loop?
    A count-controlled loop is when the code is repeated a fixed number of times (e.g.
    using a FOR loop)
    A count-controlled loop can be written as:

                                                Pseudocode

 FOR <identifier> ← <value1> TO <value2>        FOR <identifier> ← <value1> TO <value2> STEP <increment>
  <statements(s)>                                <statements(s)>
 NEXT <identifier>                              NEXT <identifier>

    In a FOR loop, the increment must be an expression that evaluates to an integer
           The loop starts at value1
           The identifier is updated by the increment value on each iteration
           The loop continues until the identifier passes value2
    The increment can be positive or negative, depending on the direction you want the
    loop to count

Nested count-controlled loops
FOR i ← 1 TO 3
  FOR j ← 1 TO 3
    OUTPUT i, " x ", j, " = ", i * j
  NEXT j
NEXT i

    The outer loop runs i from 1 to 3
    For each value of i, the inner loop runs j from 1 to 3
    It outputs all combinations of i × j
1x1=1
1x2=2

                                       © 2026 Save My Exams, Ltd.      Get more and ace your exams at savemyexams.com   5

1x3=3
2x1=2
...                                                                                                   Your notes
3x3=9

Count-controlled loops in different languages
Python                      VB.net                              Java

                                     Print the numbers 1 to 5

for i in range(1, 6):       For i As Integer = 1 To 5           for (int i = 1; i <= 5; i++) {
  print(i)                    Console.WriteLine(i)                  System.out.println(i);
                            Next                                }

Condition-controlled loops
What is a condition controlled loop?
    A condition controlled loop is when the code is repeated until a condition is met
    There are two types of condition controlled loops:
         Post-condition (REPEAT)
         Pre-condition (WHILE)

Post-condition loops (REPEAT)
    A post-condition loop is executed at least once
    The condition must be an expression that evaluates to a Boolean (True/False)
    The condition is tested after the statements are executed and only stops once the
    condition is evaluated to True
    It can be written as:

Pseudocode

REPEAT
 <statement(s)>
UNTIL <condition>

Pre-condition loops (WHILE)
    The condition must be an expression that evaluates to a Boolean (True/False)
    The condition is tested and statements are only executed if the condition evaluates to
    True
    After statements have been executed the condition is tested again

                               © 2026 Save My Exams, Ltd.           Get more and ace your exams at savemyexams.com   6

    The loop ends when the condition evaluates to False
    It can be written as:                                                                                 Your notes
 Pseudocode

 WHILE <condition> DO
  <statement(s)>
 ENDWHILE

Nested condition-controlled loops
DECLARE Username : STRING
DECLARE Count : INTEGER

Count ← 0

WHILE Count < 3
 Username ← ""

 WHILE Username = ""
   OUTPUT "Enter a username: "
   INPUT Username

   IF Username = "" THEN
     OUTPUT "Username cannot be blank."
   ENDIF
 ENDWHILE

 OUTPUT "Username accepted: ", Username
 Count ← Count + 1
ENDWHILE

    Ask the user to enter up to 3 usernames
    For each one, keep asking until a non-empty name is entered
    Outer WHILE runs 3 times (for 3 usernames)
    Inner WHILE ensures that each username is not blank

Condition-controlled loops in different languages
 Python                                          VB.net                                        Java

                                          Keep asking the user to enter a password until they type "ad

 password = ""                                   Module PasswordCheck                          import
                                                  Sub Main()
 while password != "admin":                         Dim password As String = ""                public c
  password = input("Enter the password: ")                                                       publi
                                                    While password <> "admin"                      Sca
   if password != "admin":                            Console.Write("Enter the password: ")        Str
     print("Incorrect, try again.")                   password = Console.ReadLine()
                                                                                                   wh

                                      © 2026 Save My Exams, Ltd.      Get more and ace your exams at savemyexams.com   7

                                                  If password <> "admin" Then                                S
print("Access granted!")                            Console.WriteLine("Incorrect, try again.")               p
                                                  End If                                                         Your notes
                                                End While                                                    i

                                                Console.WriteLine("Access granted!")                         }
                                                Console.ReadLine() ' Keeps the console open              }
                                              End Sub
                                            End Module                                                   Sys
                                                                                                         sca
                                                                                                     }
                                                                                                 }

When to use each type of loop
Loop type                  When to use                             Example scenario

Count-controlled           When you know in advance how            Repeating an action 5 times,
loop (FOR)                 many times you want the loop to run     generating a multiplication
                                                                   table

Pre-condition              When you want to check a              Keep asking for a valid
loop (WHILE)               condition before running the loop. It password before granting
                           may run 0 or more times               access

Post-condition             When you want the loop to run at        Ask the user for input and
loop (REPEAT ...           least once, and then stop when a        validate it after first run
UNTIL)                     condition becomes true

                                 © 2026 Save My Exams, Ltd.        Get more and ace your exams at savemyexams.com             8