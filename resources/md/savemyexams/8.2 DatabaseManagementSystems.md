# 8.2 DatabaseManagementSystems

**Course:** Cambridge International AS & A Level Computer Science (9618)
**Source:** 8.2 DatabaseManagementSystems.pdf

---

Cambridge (CIE) AS                                                             Your notes
Computer Science
Database Management Systems
(DBMS)
Contents
  DBMS features

                  © 2026 Save My Exams, Ltd.   Get more and ace your exams at savemyexams.com   1

 DBMS features
                                                                                                Your notes

DBMS vs file-based systems
What is a DBMS?
  A DBMS (Database Management System) is software that is used to create, manage,
  and interact with databases
  A DBMS provides several key features, including:
       Defines a logical schema – the overall logical design of the database (tables, fields,
       relationships, and constraints), showing how data is organised independently of
       how it is physically stored
       Provides data security and access control to keep data safe
       Maintains data integrity by enforcing rules and constraints, e.g.
           If the data is changed in one place it is updated in every other place
           Ensures each foreign key has a corresponding primary key
       Supports concurrency control so multiple users can access the database at the
       same time without errors
       Ensures data independence so changes to storage methods do not affect how
       data is used
       Offers backup and recovery tools to protect against data loss
  It addresses the limitations of file-based systems by:

Area              Limitation in file-based         How a DBMS solves it
                  systems

Data              Same data is stored in           A DBMS centralises data, so it is stored
redundancy        multiple files, wasting space    once and referenced when needed
                  and causing confusion

Data              Data may differ between files A DBMS ensures data consistency by
inconsistency     if one is updated but others  updating a single source of truth
                  are not

Data              Each application stores and      A DBMS provides a data dictionary that
management        manages its own data             defines all data elements across the
                                                   system (field/table names, data types)

                             © 2026 Save My Exams, Ltd.       Get more and ace your exams at savemyexams.com   2

Data modelling No structured way to                 A DBMS uses data modelling tools (e.g.
               represent or link data               ER diagrams) to define entities and
                                                                                                Your notes
                                                    relationships

Logical           The overall logical design of     Shows how data is organised and used,
schema            the database (tables, fields,     independently of how it is physically
                  relationships, and                stored
                  constraints)

Data integrity    Difficult to enforce validation   A DBMS uses constraints (e.g. primary
                  rules across different files      keys, foreign keys) to enforce integrity

Data security     Poor control over who can         A DBMS supports user accounts,
                  access or change data             access rights, and permissions for
                                                    individuals/groups

Backup            Often manual and                  A DBMS includes automated backup
procedures        inconsistent                      and recovery tools to reduce risk of
                                                    data loss

DBMS tools
Developer interface
  The developer interface allows developers to write database queries using Structured
  Query Language (SQL)
  This is more flexible than using query-by-example tools, which are visual and simplified
  Using SQL allows the developer to create more complex and precise queries to search,
  update, or manage data

Query processor
  The query processor is responsible for handling and executing SQL queries
  It has several key components:

Component           Function

DDL interpreter     Interprets Data Definition Language (DDL) commands like CREATE,
                    ALTER, and DROP and updates the data dictionary

DML compiler        Compiles Data Manipulation Language (DML) statements (e.g.
                    SELECT, INSERT, UPDATE) into low-level instructions and optimises the
                    query

                             © 2026 Save My Exams, Ltd.       Get more and ace your exams at savemyexams.com   3

Query evaluation    Executes the compiled instructions to retrieve or manipulate the
engine              actual data in the database
                                                                                               Your notes

  Together, these components make sure that any query written by the developer is
  interpreted, optimised, and executed efficiently

  Worked Example
  State what is meant by a data dictionary and give one example of an item typically
  found in a data dictionary. [2]
  Answer
      A data dictionary is data about the data structure in the database [1 mark]
      Example: field name, data type, or table name [1 mark]

                            © 2026 Save My Exams, Ltd.       Get more and ace your exams at savemyexams.com   4