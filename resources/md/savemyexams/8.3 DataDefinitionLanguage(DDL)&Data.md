# 8.3 DataDefinitionLanguage(DDL)&Data

**Course:** Cambridge International AS & A Level Computer Science (9618)
**Source:** 8.3 DataDefinitionLanguage(DDL)&Data.pdf

---

Cambridge (CIE) AS                                                         Your notes
Computer Science
Data Definition Language (DDL) & Data
Manipulation Language (DML)
Contents
  SQL usage

              © 2026 Save My Exams, Ltd.   Get more and ace your exams at savemyexams.com   1

 SQL usage
                                                                                              Your notes

DDL & DML
What is DDL & DML?
  Data Definition Language (DDL) is used to build and manage the structure of a relational
  database
  It defines how data is stored by creating and modifying tables, fields, indexes, and
  relationships
  Data Manipulation Language (DML) is used to manage the data stored within the
  database structures created by DDL
  It deals with adding, updating, deleting, and retrieving data
  Both DDL and DML use SQL syntax to interact with the database
  DDL structures the database, while DML maintains the contents of the database
  Most modern DBMSs use SQL to support both DDL and DML operations

Basic DDL commands
Command                Purpose                        Example

CREATE DATABASE        Creates a new database         CREATE DATABASE SchoolDB

CREATE TABLE           Creates a new table with       CREATE TABLE Students (StudentID
                       specified fields and data      INTEGER, Name VARCHAR(50))
                       types

Data types for         Defines the type of data
attributes:            each field can store

CHARACTER              Fixed-length text              Name CHARACTER(10)

VARCHAR(n)             Variable-length text (max      Email VARCHAR(100)
                       length n)

BOOLEAN                True or False value            IsActive BOOLEAN

INTEGER                Whole numbers                  Age INTEGER

REAL                   Decimal numbers                Score REAL

                            © 2026 Save My Exams, Ltd.      Get more and ace your exams at savemyexams.com   2

     DATE                      Calendar date                   DateOfBirth DATE

                                                                                                       Your notes
     TIME                      Time of day                     StartTime TIME

     ALTER TABLE               Changes an existing table       ALTER TABLE Students ADD Email
                               (e.g. add or remove fields)     VARCHAR(100)

     PRIMARY KEY (field)       Sets a field as the primary     PRIMARY KEY (StudentID) inside a
                               key (unique identifier)         CREATE TABLE or ALTER TABLE
                                                               command

     FOREIGN KEY (field)       Sets up a foreign key           FOREIGN KEY (CourseID) REFERENCES
     REFERENCES Table(Field)   relationship between tables     Courses(CourseID)

Example: School database
        You are designing a database to store information about students and the courses they
        are enrolled in
        Each student can enrol in many courses, and each course can have many students (a
        many-to-many relationship)
        Three tables are needed:
             Students – to store student details

             Courses – to store course details

             Enrolments – a link table to handle the many-to-many relationship

-- Create the Students table
CREATE TABLE Students (
     StudentID INTEGER,
     FirstName VARCHAR(50),
     LastName VARCHAR(50),
     DateOfBirth DATE,
     PRIMARY KEY (StudentID)
);

-- Create the Courses table
CREATE TABLE Courses (
  CourseID INTEGER,
     CourseName VARCHAR(100),
     Credits INTEGER,
     PRIMARY KEY (CourseID)
);

-- Create the Enrolments table to link Students and Courses
CREATE TABLE Enrolments (
  StudentID INTEGER,
     CourseID INTEGER,
     EnrolDate DATE,
     PRIMARY KEY (StudentID, CourseID),
     FOREIGN KEY (StudentID) REFERENCES Students(StudentID),

                                    © 2026 Save My Exams, Ltd.       Get more and ace your exams at savemyexams.com   3

     FOREIGN KEY (CourseID) REFERENCES Courses(CourseID)
);
                                                                                                             Your notes
Basic DML commands
Retrieving data
     Command       Purpose                   Example

     SELECT ...    Retrieves specific        SELECT FirstName, LastName FROM Students
     FROM          columns from a table

     WHERE         Filters results based     SELECT * FROM Students WHERE Age > 16
                   on a condition

     ORDER BY      Sorts the results in      SELECT * FROM Students ORDER BY LastName ASC
                   ascending or
                   descending order

     GROUP BY      Groups rows that          SELECT CourseID, COUNT(*) FROM Enrolments GROUP BY
                   have the same value in    CourseID
                   specified columns

     INNER JOIN    Combines rows from        SELECT Students.FirstName, Courses.CourseName FROM
                   two tables where          Students INNER JOIN Enrolments ON Students.StudentID =
                   there is a matching       Enrolments.StudentID INNER JOIN Courses ON
                   key                       Enrolments.CourseID = Courses.CourseID

     SUM()         Calculates the total of   SELECT SUM(Credits) FROM Courses
                   a numeric column

     COUNT()       Counts the number of      SELECT COUNT(*) FROM Enrolments
                   rows (or non-null
                   entries in a column)

     AVG()         Calculates the            SELECT AVG(Age) FROM Students
                   average value of a
                   numeric column

Data maintenance
     Command       Purpose                     Example

     INSERT INTO   Adds a new record to a      INSERT INTO Students (StudentID, FirstName, LastName)
                   table                       VALUES (1, 'Ali', 'Khan')

                                   © 2026 Save My Exams, Ltd.              Get more and ace your exams at savemyexams.com   4

 DELETE            Removes records from              DELETE FROM Students WHERE StudentID = 1
 FROM              a table
                                                                                                           Your notes
 UPDATE            Modifies existing data            UPDATE Students SET Age = 17 WHERE StudentID = 1
                   in a table

Example: School database
     You're working with a school database containing three tables:
          Students(StudentID, FirstName, LastName, Age)

          Courses(CourseID, CourseName, Credits)

          Enrolments(StudentID, CourseID, EnrolDate)

     You want to query the database and maintain the data as students enrol in courses,
     update their details, or leave the school
Querying
-- 1. Get the first and last names of all students
SELECT FirstName, LastName FROM Students;

-- 2. List students who are older than 16
SELECT * FROM Students WHERE Age > 16;

-- 3. List all students sorted alphabetically by surname
SELECT * FROM Students ORDER BY LastName ASC;

-- 4. Count how many students are enrolled on each course
SELECT CourseID, COUNT(*) FROM Enrolments GROUP BY CourseID;

-- 5. Show student names with the courses they are enrolled on
SELECT Students.FirstName, Students.LastName, Courses.CourseName
FROM Students
INNER JOIN Enrolments ON Students.StudentID = Enrolments.StudentID
INNER JOIN Courses ON Enrolments.CourseID = Courses.CourseID;

-- 6. Get the total number of course credits available
SELECT SUM(Credits) FROM Courses;

-- 7. Get the average age of all students
SELECT AVG(Age) FROM Students;

Maintaining
-- 1. Add a new student to the Students table
INSERT INTO Students (StudentID, FirstName, LastName, Age)
VALUES (4, 'Amira', 'Patel', 17);

-- 2. Enrol that student on CourseID 2
INSERT INTO Enrolments (StudentID, CourseID, EnrolDate)
VALUES (4, 2, '01/09/2025'); -- UK date format (DD/MM/YYYY)

-- 3. Update a student's age
UPDATE Students SET Age = 18 WHERE StudentID = 4;

                                    © 2026 Save My Exams, Ltd.           Get more and ace your exams at savemyexams.com   5

-- 4. Delete a student who has left the school
DELETE FROM Students WHERE StudentID = 4;                                                         Your notes

    Worked Example
    Part of a database table BIRD_TYPE is shown:

         BirdID                  Name                           Size

         0123                    Blackbird                      Medium

         0035                    Jay                            Large

         0004                    Raven                          Large

         0085                    Robin                          Small

    The database only supports these data types:
        character
        varchar
        Boolean
        integer
        real
        date
        time
    Write a Structured Query Language (SQL) script to define the table Bird_Type. [4]
    Answer
            CREATE TABLE start and end bracket [1 mark]
       Bird ID as CHAR/VARCHAR [1 mark]
       Name and size as VARCHAR/CHAR [1 mark]
       Bird ID as primary key [1 mark]
    Example answer
    CREATE TABLE BIRD_TYPE(
    BirdID CHAR(4) NOT NULL,
    Name VARCHAR(9),
    Size VARCHAR(6),
    PRIMARY KEY (BirdID)
    );

                                   © 2026 Save My Exams, Ltd.   Get more and ace your exams at savemyexams.com   6